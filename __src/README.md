```
touch gulpfile.js
touch gulpfile.coffee

npm init
```

### Bower

```
npm install -g bower

bower init
bower install --save jquery
bower install --save angular

bower list
```

### NPM

```
npm install --save gulp
npm install --save gulp-util
npm install --save gulp-concat

npm install --save gulp-coffee
npm install --save coffee-script

npm install --save main-bower-files
npm install --save gulp-uglify

npm install --save gulp-sass
npm install --save gulp-minify-css
```

```
npm install --save gulp-postcss
npm install --save autoprefixer-core
npm install --save postcss-opacity
```

```
npm install --save gulp-plumber
npm install --save gulp-batch
npm install --save chokidar

npm install --save gulp-jade
npm install --save gulp-minify-html
```

### Gulp

```
./node_modules/gulp/bin/gulp.js
```
